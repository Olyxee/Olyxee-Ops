OPS_DATABASE_URL=postgresql://user:password@host:5432/ops_database
EXTERNAL_DATABASE_URL=postgresql://user:password@host:5432/people_database
SESSION_SECRET=replace-with-a-long-random-production-secret

# Use pooled PostgreSQL connection URLs for serverless deployments.
# Keep all real values in deployment secrets and never commit them.